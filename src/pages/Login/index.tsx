import { FormLogin } from "../../components/FormLogin";

const Login = () => (
    <main>
        <div>
            <div>
                <FormLogin />
            </div>
        </div>
    </main>
);
export default Login;
